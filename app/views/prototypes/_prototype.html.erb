<div class="card">
  <%= link_to image_tag(prototype.image), prototype_path(prototype.id), class: :card__img %> 
  <div class="card__body">
    <%= link_to "title",prototype_path(prototype.id),class: :card__title %>
    <p class="card__summary">
      <%= "プロトタイプのキャッチコピー" %>
    </p>
    <%= link_to "by プロトタイプの投稿者名", prototype_path(prototype.id), class: :card__user %>
  </div>
</div>

 